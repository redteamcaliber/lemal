Copy the filename into the same directory as the script. Edit the script to extract the sections from this new file. Signatures are produced in the ClamAV format using both the MD5 and SHA256 hashing algorithms.
